# this file defines the list of APT dependencies for ROS packages
#
# ros_package_dependency:
#   ubuntu: [apt dependencies]
#

# px4 is build using a custom script outside of the bloom pipeline
px4:
  ubuntu: [ros-noetic-px4]

# libcamera_ros is build using a custom script outside of the bloom pipeline
libcamera_ros:
  ubuntu: [ros-noetic-libcamera-ros]

# non-ROS dependencies managed by MRS
mrs-geographiclib-datasets:
  ubuntu: [mrs-geographiclib-datasets]
mrs-uav-shell-additions:
  ubuntu: [mrs-uav-shell-additions]

# TODO delete when part of the MRS pipeline
mrs_robofly_core:
  ubuntu: [ros-noetic-mrs-robofly-core]

# TODO delete when part of the MRS pipeline (VINS-Mono)
camera_model:
  ubuntu: [ros-noetic-camera-model]
mrs-vins-mono-estimator-plugin:
  ubuntu: [ros-noetic-mrs-vins-mono-estimator-plugin]

# other dependencies
python3-jinja2:
  ubuntu: [python3-jinja2]
python-is-python3:
  ubuntu: [python-is-python3]
tmuxinator:
  ubuntu: [tmuxinator]
moreutils:
  ubuntu: [moreutils]
libgbm-dev:
  ubuntu: [libgbm-dev]
aptitude:
  ubuntu: [aptitude]
gdb:
  ubuntu: [gdb]
